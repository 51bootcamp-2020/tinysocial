module.exports.EventSchedule = {
  id: ({id}) => id,
  start: ({start}) => start,
  end: ({end}) => end,
  locationLatitude: ({locationLatitude}) => locationLatitude,
  locationLongitude: ({locationLongitude}) => locationLongitude,
  city: ({city}) => city,
  state: ({state}) => state,
};